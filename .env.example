# Shared environment variables
# Copy to .env and fill in values for local development

# PostgreSQL (future backend use)
POSTGRES_USER=aiteam
POSTGRES_PASSWORD=changeme
POSTGRES_DB=aiteam

# Future backend API
NEXT_PUBLIC_API_URL=http://localhost:8080
